export enum ErrorCode {
  none = 0,
  common = 1,
  enum = 10,
  existed = 11,
  params = 12,
  // protocol error 1000 start

  // network error 2000 start

  // ui error 3000 start

  // dev error 10000 start
}
